# 一番賞 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增一番賞模式:A~G 七級賞別、抽到就消耗、抽走最後一張額外送最後一抽賞。

**Architecture:** 純邏輯放 `ichiban/js/ichiban.js`(可用 `node --test` 直接跑),畫面是一疊可以點開的籤紙。

**Tech Stack:** 原生 ES module、零建置、零 npm 依賴。測試用 Node 內建 `node --test`。

**Spec:** `docs/superpowers/specs/2026-09-22-multi-mode-site-design.md`

**Prerequisite:** `2026-09-22-00-foundation.md` 必須先完成 —— 本計畫用到 `shared/js/` 的 ids、roster、storage、dialog、sound、ask。

## Global Constraints

- **零建置**:沒有 build step、沒有 `node_modules`、沒有 `npm install`。`package.json` 一律不新增。
- **唯一的外部依賴**是 three.js,只能出現在 `gashapon3d/index.html` 的 importmap,從 `https://cdn.jsdelivr.net/npm/three@0.169.0/build/three.module.js` 載入。
- 測試一律 `cd /Users/willian/github/gashapon && node --test`,必須零依賴可跑。
- 所有純函式**不就地改動傳進來的東西**,一律回傳新物件。這是現有 `gashapon/js/state.js` 的既有慣例。
- 讀 localStorage 一律當成不可信:壞掉就回種子資料,**絕不白屏**。
- 文案一律繁體中文;程式碼、識別字、commit message 一律英文。
- commit message 結尾必須有:
  `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`
- 現有 2D 扭蛋機的 45 個測試在整個計畫過程中**不得有任何一個變紅**。

## 確認過的 Model(權威來源;實作若與此牴觸,是計畫的 bug —— 停下來反映,不要就地改模型)

### Entities

- `AppPrefs { soundOn }` —— 全域,獨立 storage key
- 每個模式都有 `setups[]` 與 `activeSetupId`
- 扭蛋機 2D:`Prize { id, name, count, rarity }` → `Capsule { prizeId, drawn }`
- 扭蛋機 3D:`Prize { id, name, count, rarity }` → `Capsule { prizeId, drawn, color }`
- 一番賞:`IchibanPrize { id, name, tier(A~G), count }` + `lastOnePrize` → `Ticket { prizeId, drawn }`
- 阿彌陀籤:`Player { id, name, color }` + `GhostPrize { id, name, count }` → `Ladder`(每局現場產生,不存)
- 大亂鬥:`Fighter { id, name, color, count }` + `items{attack,speed,giant,bomb}` → `Combatant { fighterId, alive }`(不存)

### Cardinality

- `Prize` 1:N `Capsule`、`IchibanPrize` 1:N `Ticket`、`GhostPrize` 1:N 底部格子、
  `Fighter` 1:N `Combatant` —— 全部由 `count` 決定,同一個形狀
- `Player` 1:1 梯子上的一條線
- **`Fighter` 就是隊伍**,`count` 就是隊伍人數。同 `fighterId` 的 `Combatant` 不互相擊飛,
  最後還有人活著的 `fighterId` 獲勝

### Seen vs stored

- 3D 看到球體裡五顏六色的蛋 → **顏色是每顆蛋自己隨機挑的**,跟獎項、跟稀有度都無關
- 一番賞看到「A賞 PVC模型」→ 存的是 `tier: 'A'` 欄位,**不是名字前綴**
- 阿彌陀籤看到大家像賽跑有快有慢 → **誰拿到什麼在起跑前梯子就決定好了**,快慢純粹是演出
- 阿彌陀籤底部補的「銘謝惠顧」空籤是**算出來的**,不存進設定
- 大亂鬥是**唯一**結果不預先決定的模式

### 最可能做錯的四個(以下四句都是錯的)

1. 「一番賞的賞別就是稀有度」 —— **一番賞沒有稀有度,扭蛋機沒有賞別**
2. 「大亂鬥的隊伍是一個獨立實體」 —— 沒有 `Team`,隊伍就是同一筆 `Fighter`
3. 「3D 扭蛋機的蛋色由稀有度決定」 —— 只有 2D 那台是。3D 每顆蛋隨機上色
4. 「阿彌陀籤誰先跑到終點誰拿頭獎」 —— 名次跟獎項完全無關

## File Structure

| 路徑 | 職責 |
|---|---|
| `ichiban/js/ichiban.js` | `TIERS`、抽籤、最後一抽判定(純函式) |
| `ichiban/js/store.js` | `ichiban.v1` 的讀寫與容錯 |
| `ichiban/js/ui.js` | 籤紙桌面與翻開演出 |
| `ichiban/js/main.js` | 接線 |
| `ichiban/index.html` `ichiban/css/ichiban.css` | 畫面 |

---

### Task 1: 一番賞的純邏輯

**Files:**
- Create: `ichiban/js/ichiban.js`
- Test: `test/ichiban.test.js`

**Interfaces:**
- Consumes: `newId` from `shared/js/ids.js`;`expand`, `countOf`, `createSetupList` from `shared/js/roster.js`
- Produces:
  - `TIERS: ['A','B','C','D','E','F','G']`
  - `TIER_META: { A: { label: 'A賞', color, glow }, … }`
  - `createIchibanPrize({ name?, tier?, count? }): IchibanPrize`
  - `createIchibanSetup({ name?, prizes?, lastOnePrize? }): IchibanSetup`
  - `buildTickets(prizes): Ticket[]`
  - `drawTicket(setup, rng?): { ticket, prize, tickets, isLastOne, lastOnePrize } | null`
  - `refillSetup(setup): IchibanSetup`

`drawTicket` 從還沒抽的籤裡均勻隨機挑一張,標記 `drawn`,回傳**新的** `tickets`。
抽走的是最後一張、且 `setup.lastOnePrize` 去空白後非空時,`isLastOne` 為 `true`、
`lastOnePrize` 帶該字串;否則 `isLastOne` 為 `false`、`lastOnePrize` 為 `null`。

- [ ] **Step 1: 寫失敗的測試**

建立 `test/ichiban.test.js`:

```js
import test from 'node:test';
import assert from 'node:assert/strict';

import {
  TIERS, createIchibanPrize, createIchibanSetup,
  buildTickets, drawTicket, refillSetup,
} from '../ichiban/js/ichiban.js';
import { remaining } from '../shared/js/roster.js';

function seeded(seed) {
  let a = seed >>> 0;
  return () => {
    a = (a + 0x6D2B79F5) >>> 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

test('賞別固定是 A 到 G 七級', () => {
  assert.deepEqual(TIERS, ['A', 'B', 'C', 'D', 'E', 'F', 'G']);
});

test('一番賞的獎項沒有稀有度這個欄位', () => {
  const prize = createIchibanPrize({ name: '模型', tier: 'A', count: 1 });
  assert.equal('rarity' in prize, false, '一番賞不該有 rarity');
  assert.equal(prize.tier, 'A');
});

test('不認得的賞別退回 G(最低階)', () => {
  assert.equal(createIchibanPrize({ name: 'x', tier: 'Z', count: 1 }).tier, 'G');
});

test('buildTickets 依數量展開', () => {
  const prizes = [
    createIchibanPrize({ name: '模型', tier: 'A', count: 1 }),
    createIchibanPrize({ name: '吊飾', tier: 'C', count: 3 }),
  ];
  const tickets = buildTickets(prizes);
  assert.equal(tickets.length, 4);
  assert.ok(tickets.every(t => t.drawn === false));
});

test('抽一張會消耗一張,而且不動到獎項的 count', () => {
  const setup = createIchibanSetup({
    prizes: [createIchibanPrize({ name: '模型', tier: 'A', count: 3 })],
  });
  const result = drawTicket(setup, seeded(1));
  assert.equal(setup.prizes[0].count, 3, 'prizes 是設定,不該被消耗');
  assert.equal(result.tickets.filter(t => t.drawn).length, 1);
  assert.equal(remaining(setup.tickets), 3, 'drawTicket 不該就地改動 setup');
});

test('抽光了就回 null', () => {
  let setup = createIchibanSetup({
    prizes: [createIchibanPrize({ name: '模型', tier: 'A', count: 2 })],
  });
  const rng = seeded(2);
  for (let i = 0; i < 2; i++) {
    const r = drawTicket(setup, rng);
    assert.ok(r !== null);
    setup = { ...setup, tickets: r.tickets };
  }
  assert.equal(drawTicket(setup, rng), null);
});

test('抽走最後一張才觸發最後一抽賞', () => {
  let setup = createIchibanSetup({
    prizes: [createIchibanPrize({ name: '模型', tier: 'A', count: 3 })],
    lastOnePrize: '特別大獎',
  });
  const rng = seeded(3);
  const seen = [];
  for (let i = 0; i < 3; i++) {
    const r = drawTicket(setup, rng);
    seen.push(r.isLastOne);
    setup = { ...setup, tickets: r.tickets };
  }
  assert.deepEqual(seen, [false, false, true]);
});

test('最後一抽賞是空字串時,抽完也不觸發', () => {
  let setup = createIchibanSetup({
    prizes: [createIchibanPrize({ name: '模型', tier: 'A', count: 1 })],
    lastOnePrize: '   ',
  });
  const r = drawTicket(setup, seeded(4));
  assert.equal(r.isLastOne, false);
  assert.equal(r.lastOnePrize, null);
});

test('中獎機率只看數量,賞別不參與', () => {
  const build = (t1, t2) => createIchibanSetup({
    prizes: [
      createIchibanPrize({ name: 'A', tier: t1, count: 3 }),
      createIchibanPrize({ name: 'B', tier: t2, count: 1 }),
    ],
  });
  const seq = (setup, seed) => {
    const rng = seeded(seed);
    const out = [];
    let s = setup;
    for (let i = 0; i < 4; i++) {
      const r = drawTicket(s, rng);
      out.push(r.prize.name);
      s = { ...s, tickets: r.tickets };
    }
    return out;
  };
  assert.deepEqual(seq(build('A', 'G'), 9), seq(build('G', 'A'), 9));
});

test('refillSetup 把籤全部放回去,不動 prizes', () => {
  const setup = createIchibanSetup({
    prizes: [createIchibanPrize({ name: '模型', tier: 'A', count: 2 })],
  });
  const used = { ...setup, tickets: setup.tickets.map(t => ({ ...t, drawn: true })) };
  const refilled = refillSetup(used);
  assert.equal(remaining(refilled.tickets), 2);
  assert.deepEqual(refilled.prizes, setup.prizes);
});
```

- [ ] **Step 2: 跑測試確認它失敗**

Run: `cd /Users/willian/github/gashapon && node --test`
Expected: FAIL,`Cannot find module '.../ichiban/js/ichiban.js'`

- [ ] **Step 3: 寫最小實作**

建立 `ichiban/js/ichiban.js`:

```js
// 一番賞。注意:這裡沒有稀有度 —— 一番賞用的是「賞別」,兩者是不同的東西。
import { newId } from '../../shared/js/ids.js';
import { expand } from '../../shared/js/roster.js';

export const TIERS = Object.freeze(['A', 'B', 'C', 'D', 'E', 'F', 'G']);

// 外層凍住還不夠——每個賞別的 meta 是巢狀物件,outer freeze 擋不住
// TIER_META.A.color = 'x' 這種改法,所以每個 value 也要各自凍。
export const TIER_META = Object.freeze(Object.fromEntries(
  Object.entries({
    A: { label: 'A賞', color: '#FF6F91', glow: 'rgba(255,111,145,.9)' },
    B: { label: 'B賞', color: '#FFA45B', glow: 'rgba(255,164,91,.85)' },
    C: { label: 'C賞', color: '#FFD479', glow: 'rgba(255,212,121,.8)' },
    D: { label: 'D賞', color: '#9BE7C4', glow: 'rgba(155,231,196,.8)' },
    E: { label: 'E賞', color: '#8CC9FF', glow: 'rgba(140,201,255,.75)' },
    F: { label: 'F賞', color: '#C4A2FF', glow: 'rgba(196,162,255,.7)' },
    G: { label: 'G賞', color: '#E7DFD4', glow: 'rgba(231,223,212,.7)' },
  }).map(([tier, meta]) => [tier, Object.freeze(meta)]),
));

export function createIchibanPrize({ name = '新獎項', tier = 'G', count = 1 } = {}) {
  return {
    id: newId('ip'),
    name,
    tier: TIERS.includes(tier) ? tier : 'G',
    // count 不可信:Infinity / NaN 會讓 buildTickets 依賴的 expand 迴圈爆掉,
    // 一律當成 0(沒有籤)比「憑空多一顆」安全。
    count: Number.isFinite(count) ? Math.max(0, Math.floor(count)) : 0,
  };
}

export function buildTickets(prizes) {
  return expand(prizes, prize => ({ prizeId: prize.id, drawn: false }));
}

export function createIchibanSetup({ name = '我的一番賞', prizes = [], lastOnePrize = '' } = {}) {
  return { id: newId('is'), name, prizes, lastOnePrize, tickets: buildTickets(prizes) };
}

export function refillSetup(setup) {
  return { ...setup, tickets: buildTickets(setup.prizes) };
}

// 抽一張。機率只由 count 決定 —— 每張籤機會均等,賞別從不參與計算。
export function drawTicket(setup, rng = Math.random) {
  const candidates = [];
  setup.tickets.forEach((ticket, index) => { if (!ticket.drawn) candidates.push(index); });
  if (candidates.length === 0) return null;

  const index = candidates[Math.floor(rng() * candidates.length)];
  const ticket = setup.tickets[index];
  const prize = setup.prizes.find(p => p.id === ticket.prizeId);
  const tickets = setup.tickets.map((t, i) => (i === index ? { ...t, drawn: true } : t));

  const bonus = (setup.lastOnePrize ?? '').trim();
  const wasLast = candidates.length === 1 && bonus !== '';

  return {
    ticket,
    prize,
    tickets,
    isLastOne: wasLast,
    lastOnePrize: wasLast ? bonus : null,
  };
}
```

- [ ] **Step 4: 跑測試確認全綠**

Run: `cd /Users/willian/github/gashapon && node --test`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add ichiban/js/ichiban.js test/ichiban.test.js
git commit -m "$(cat <<'MSG'
Add the ichiban kuji logic

Tiers A through G, drawn without replacement, with the last-one prize
firing only on the ticket that empties the box. A test asserts the prize
object carries no rarity field: the tier is its own thing, and conflating
the two is the mistake this model is most likely to invite.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
MSG
)"
```

---

### Task 2: 一番賞的畫面與演出

**Files:**
- Create: `ichiban/index.html`
- Create: `ichiban/css/ichiban.css`
- Create: `ichiban/js/ui.js`
- Create: `ichiban/js/main.js`
- Create: `ichiban/js/store.js`

**Interfaces:**
- Consumes: `ichiban/js/ichiban.js` 全部匯出;`createStore`、`createSetupList`/`getActive`/`replaceSetup`/`addSetup`/`removeSetup`/`needsRebuild`/`entriesChanged`、`createDialogShell`、`createAsk`、`sound`、`loadPrefs`/`savePrefs`
- Produces: 可獨立運作的 `/ichiban/` 頁

畫面:一疊籤紙鋪在桌上,每張隨機旋轉 `-12deg ~ 12deg`、隨機位移,點一張翻開。
`TIER_META[tier].color` 決定翻開後籤紙的顏色;A 賞的爆光最盛大、G 賞最樸素(用 `TIERS.indexOf` 反轉當作等級)。
抽走最後一張且 `isLastOne` 為真時,結果卡關掉後再跳一張金色的「最後一抽賞」卡。

設定對話框三個分頁:**剩下什麼**(每個獎項 `剩餘/總數` + 裝滿重來)、
**編輯獎項**(名稱 input + 七個賞別色點 + 數量 ∓ + 刪除 + 新增)、
**其他**(一番賞名字、最後一抽賞的名字、音效)。

- [ ] **Step 1: 寫 `ichiban/js/store.js`**

```js
import { createStore } from '../../shared/js/storage.js';
import { createSetupList } from '../../shared/js/roster.js';
import { createIchibanSetup, createIchibanPrize, buildTickets, TIERS } from './ichiban.js';

const SEED_PRIZES = [
  { name: '大獎',   tier: 'A', count: 1 },
  { name: '二獎',   tier: 'B', count: 2 },
  { name: '三獎',   tier: 'C', count: 3 },
  { name: '小獎',   tier: 'E', count: 6 },
  { name: '銘謝惠顧', tier: 'G', count: 8 },
];

export function seedState() {
  return createSetupList(createIchibanSetup({
    name: '我的一番賞',
    prizes: SEED_PRIZES.map(createIchibanPrize),
    lastOnePrize: '最後一抽大獎',
  }));
}

function sanitizeSetup(raw) {
  if (!raw || typeof raw !== 'object' || typeof raw.id !== 'string') return null;
  const prizes = Array.isArray(raw.prizes)
    ? raw.prizes.filter(p => p && typeof p.id === 'string').map(p => ({
        id: p.id,
        name: typeof p.name === 'string' ? p.name : '獎項',
        tier: TIERS.includes(p.tier) ? p.tier : 'G',
        count: Number.isFinite(p.count) ? Math.max(0, Math.floor(p.count)) : 1,
      }))
    : [];
  const ids = new Set(prizes.map(p => p.id));
  const ok = Array.isArray(raw.tickets) && raw.tickets.every(t => t && ids.has(t.prizeId));
  return {
    id: raw.id,
    name: typeof raw.name === 'string' ? raw.name : '我的一番賞',
    lastOnePrize: typeof raw.lastOnePrize === 'string' ? raw.lastOnePrize : '',
    prizes,
    tickets: ok ? raw.tickets.map(t => ({ prizeId: t.prizeId, drawn: t.drawn === true })) : buildTickets(prizes),
  };
}

export const store = createStore({
  key: 'ichiban.v1',
  schema: 1,
  seed: seedState,
  sanitize: parsed => {
    if (!Array.isArray(parsed.setups)) return null;
    const setups = parsed.setups.map(sanitizeSetup).filter(Boolean);
    if (setups.length === 0) return null;
    const activeSetupId = setups.some(s => s.id === parsed.activeSetupId)
      ? parsed.activeSetupId : setups[0].id;
    return { setups, activeSetupId };
  },
});
```

- [ ] **Step 2: 寫 `ichiban/index.html`、`ichiban/css/ichiban.css`、`ichiban/js/ui.js`、`ichiban/js/main.js`**

⚠️ **共用層的現況(計畫寫成時還沒實作,以下以實際檔案為準)**:

- `shared/css/tokens.css` **已經提供** `body` 的背景/字體/顏色、`.btn` 系列、`.icon-btn`、
  `.dialog` 系列、`.dialog--ask` 與 `.ask__*`、`.tabs`/`.tab`/`.panel`、`.field`、`.switch`、
  `.chip` 與 `.chip--danger`、`.picker`、`.home-link`。
  **`css/ichiban.css` 不要重新定義它們**,只寫一番賞自己的東西(籤紙桌面、翻牌演出、賞別配色)。
- `createDialogShell` 多了一個**可選的 `onSetupAdded`** callback,而且**不再自動跳到最後一個分頁**。
  一番賞要傳它,做:跳到「其他」分頁 + `nameInput.focus()` + `select()`。
  不傳的話新增一組設定之後游標不在名字框裡。
- `shared/js/roster.js` 有泛用的 `remaining(items)`(`filter(i => !i.drawn).length`)。
  **用它,不要自己再寫一支 `remainingTickets`。**
- `index.html` 的 `.topbar` 要有 `<a class="home-link" href="../" aria-label="回首頁">←</a>`,
  樣式已經在 tokens.css 裡。五個模式都要有,不要漏。

結構比照 `gashapon/`:`index.html` 載 `../shared/css/tokens.css` 與 `css/ichiban.css`,
`<script type="module" src="js/main.js">`。`ui.js` 負責桌面渲染與翻籤演出,`main.js` 負責接線與 store。
翻籤演出用 Web Animations API,並沿用 2D 扭蛋機 `reveal.js` 既有的三條規則:
**每一步自己掌握 promise 的解除時機**(動畫結束 / 按了跳過 / 逾時保險,先到先算)、
**`reset()` 先 `cancel()` 掉上一次建立的動畫**(`fill: 'forwards'` 會蓋過 inline style)、
**分頁轉背景時自動快轉**。

- [ ] **Step 3: 跑測試**

Run: `cd /Users/willian/github/gashapon && node --test`
Expected: PASS

- [ ] **Step 4: 手動確認**

開 `http://localhost:8000/ichiban/`:
籤紙散落自然、點一張會翻開、A 賞的演出明顯比 G 賞盛大、
抽到最後一張會跳最後一抽賞、重新整理後進度還在、設定三個分頁都正常。

- [ ] **Step 5: Commit**

```bash
git add ichiban/
git commit -m "$(cat <<'MSG'
Add the ichiban kuji screen

A scattered pile of tickets; picking one flips it over. The tier drives
the colour and how big a fuss the reveal makes, and emptying the box
brings out the last-one prize on its own card.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
MSG
)"
```

---

---

### Task 3: 把翻牌演出換成「沿著一條裂痕撕開」

**Files:**
- Modify: `ichiban/index.html`(`.reveal-stack` 裡面整段換掉)
- Modify: `ichiban/css/ichiban.css`(刪掉所有 `.ticket-card*` 規則;`.ticket` 桌面卡改樣式;新增 `.tear*`)
- Modify: `ichiban/js/ui.js`(`createDeskView` 的顏色、`createRevealer` 的 `reset`/`playHold`/`playTear`/`spawnShreds`)
- Modify: `ichiban/js/main.js`(只改 `createRevealer({...})` 傳進去的 els)

**Interfaces:**
- Consumes:`ichiban/js/ichiban.js` 的 `TIERS`/`TIER_META`;`shared/js/sound.js` 的 `sfx`
- Produces:`createRevealer` 的對外介面**完全不變** —— 仍然是
  `{ isPlaying, requestSkip(), hold({tier}, originRect), cancelReturn(originRect), tear({name, tier}), playBonus(name), clear() }`。
  `main.js` 除了 els 之外一行都不該動。

#### 為什麼要改(不要當成「微調」)

現況是**票卡橫向裂成上下兩半、上半片往上飛走**。使用者看過真品之後說「現在沒撕開的感覺」。
下面這份規格不是想像的,是逐格看 `https://www.gachago.net/zh-tw/Ichiban` 的撕開動畫記下來的。

實際觀察到的機制,照順序:

1. **未撕的票卡**:橫式圓角矩形,外框是一圈**很粗的深色描邊**,底下有陰影;
   卡面填賞別色;中間一個內縮的淺色面板,面板四邊是**一排短齒(虛線)**;
   **左邊緣咬進去一個半圓缺口**;卡面上**沒有任何文字**。下面是「取消」「撕開」兩顆按鈕。
2. **按下撕開**:彩色外殼從靠左約 1/3 的位置**裂成左右兩片**,兩片**往相反方向平移並各自微微轉一點角度**,
   中間的縫愈開愈大,縫裡露出**底下那張白紙**。白紙上的獎項圖案就是這樣一點一點被讓出來的。
3. 裂邊沒有描邊(撕痕是紙的斷面,不是印上去的線),但**外殼會在白紙上投影**,所以看得出厚度。
4. 兩片外殼接著**各自飛出畫面**,白紙留在正中央放大、定住。
5. 最後:獎項在中央,下面一行「B獎」色籤 + 獎項名,再下面是「取消」「確定」。

**關鍵在第 2 點**:「像撕」來自**一條裂痕兩邊分開**,不是「一塊飛走」。
上一版之所以不像,是因為它是「掀掉一半」。

**不需要 canvas。** gachago 用 canvas 畫是它的選擇,上面五點用 DOM + `clip-path` 全部做得到。

#### 這一版跟 gachago 刻意不一樣的地方

**桌上的籤紙不能透露賞別。** gachago 桌上每張卡顏色都不同,我們不能照抄 —— 小孩會直接挑金色那張。
桌面用一組**固定的裝飾色**依 index 輪流上,**跟 `TIER_META[tier].color` 完全無關**;
賞別色只在票卡飛到中央之後才出現。

- [ ] **Step 1:`ichiban/index.html` —— 換掉 `.reveal-stack` 的內容**

把 `<div class="reveal-stack">` 裡從 `<div class="ticket-card" id="ticketCard">` 到它的 `</div>`
整段(也就是 `.aura` 之前的全部)換成:

```html
      <div class="tear" id="tearCard">
        <div class="tear__paper">
          <div class="tear__result" id="cardResult">
            <span class="tear__badge" id="cardBadge"></span>
            <p class="tear__name" id="cardName"></p>
          </div>
        </div>
        <span class="tear__stub" aria-hidden="true"></span>
        <div class="tear__shell" id="shellLeft" aria-hidden="true"><span class="tear__perf"></span></div>
        <div class="tear__shell" id="shellRight" aria-hidden="true"><span class="tear__perf"></span></div>
      </div>
```

`.aura`、`.particles`、`.ticket-actions` 原封不動留著。
`id="cardResult"`、`cardBadge`、`cardName` 也刻意沿用舊名字,`main.js` 那幾行就不用動。

- [ ] **Step 2:`ichiban/css/ichiban.css` —— 刪掉舊的、加上新的**

刪掉所有選擇器含 `.ticket-card` 的規則(含 `.ticket-card__half`、`__lines`、`__perf`、`__notch`、
`__result`、`__badge`、`__name`,以及 `[data-bonus="true"]` 那條)。
`.reveal-stack`、`.aura`、`.particles`、`.particle`、`.shred`、`.ticket-actions` **留著不要動**。

把 `.ticket`(桌上的籤紙)改成票卡的長相 —— 原本那條 `.ticket { ... }` 的 `background` 換掉、
其餘保留,並加上內框:

```css
/* 桌上的籤紙就是票卡的縮小版:粗描邊 + 一圈齒孔。
   顏色是 ui.js 給的裝飾色,不是賞別色 —— 賞別在撕開前不能外洩。 */
.ticket { background: var(--card-color, var(--cream-deep)); }

.ticket__mark {
  position: absolute;
  inset: 7px;
  border-radius: 8px;
  border: 2px dashed rgba(255, 255, 255, .8);
  background: rgba(255, 255, 255, .3);
  font-size: 0;
}
```

(`.ticket` 已經是 `position: relative` 的話就不用再加;不是的話補上 `position: relative;`。)

新增撕開演出的樣式:

```css
/* 撕開演出:白色籤心在下,彩色外殼在上、裂成左右兩片。
   兩片相鄰的那條邊是同一組點做出來的互補 clip-path,
   所以未撕之前嚴絲合縫,撕開才像沿著同一條裂痕分開。 */
.tear {
  position: absolute;
  left: 50%;
  top: 50%;
  width: min(80vw, 340px);
  aspect-ratio: 34 / 15;
  translate: -50% -50%;
  opacity: 0;
}

.tear__paper {
  position: absolute;
  inset: 0;
  display: grid;
  place-items: center;
  border: 5px solid var(--ink);
  border-radius: 20px;
  background: var(--cream);
  box-shadow: 0 6px 0 rgba(87, 66, 57, .22);
  overflow: hidden;
}

/* 左邊緣咬進去的半圓缺口 —— 一番賞票卡的招牌記號。 */
.tear__stub {
  position: absolute;
  left: -13px;
  top: 50%;
  width: 26px;
  height: 26px;
  margin-top: -13px;
  border: 5px solid var(--ink);
  border-radius: 50%;
  background: var(--cream);
  z-index: 3;
}

.tear__shell {
  position: absolute;
  inset: 0;
  z-index: 2;
  border: 5px solid var(--ink);
  border-radius: 20px;
  background: var(--tier-color, #E7DFD4);
  /* drop-shadow 吃的是 clip-path 之後的輪廓,鋸齒邊才會在白紙上投影、看得出厚度。
     box-shadow 不行:它畫的是沒被裁的矩形。 */
  filter: drop-shadow(0 0 5px rgba(87, 66, 57, .45));
}

/* 票面的騎縫框。這個尺寸下虛線看起來就是一排齒孔。 */
.tear__perf {
  position: absolute;
  inset: 14px;
  border: 3px dashed rgba(255, 255, 255, .75);
  border-radius: 12px;
  background: rgba(255, 255, 255, .26);
}

.tear__result {
  padding: 0 18px;
  text-align: center;
  opacity: 0;
}

.tear__badge {
  display: inline-block;
  padding: 3px 12px;
  border-radius: 999px;
  background: var(--tier-color, #E7DFD4);
  color: var(--ink);
  font-size: 15px;
  font-weight: 800;
}

.tear__name {
  margin: 8px 0 0;
  font-size: clamp(20px, 6vw, 30px);
  font-weight: 900;
  line-height: 1.2;
  overflow-wrap: anywhere;
}

/* 最後一抽賞:白紙換成金邊,跟一般的賞別分得開。 */
.tear[data-bonus="true"] .tear__paper,
.tear[data-bonus="true"] .tear__stub { border-color: #B8860B; }
```

- [ ] **Step 3:`ichiban/js/ui.js` —— 桌面卡片的裝飾色**

在檔案上方 `GOLD` 旁邊加:

```js
// 桌上籤紙的裝飾色。故意跟 TIER_META 無關 —— 賞別在撕開之前不能從桌面看出來。
const DESK_COLORS = Object.freeze([
  '#F6C6B8', '#C9B6E4', '#BBD9F0', '#F7DFA0', '#BFE3C8', '#F3B8CE', '#E8CFAE',
]);
```

`createDeskView` 的 `render` 裡,建完 `btn` 之後加一行:

```js
      btn.style.setProperty('--card-color', DESK_COLORS[i % DESK_COLORS.length]);
```

- [ ] **Step 4:`ichiban/js/ui.js` —— 產生鋸齒裂痕**

在 `createRevealer` 內、`reset()` 之前加:

```js
  // 撕痕。同一組點同時餵給左右兩片:左片取點的左邊、右片取點的右邊,
  // 合起來剛好是完整的矩形,分開時斷面自然互補。
  // 齒的正負號逐點交替,才是鋸齒而不是一條抖動的曲線。
  let tearAt = 32;

  function applyTearLine(seed) {
    tearAt = 26 + pseudoRandom(seed) * 22; // 撕點落在 26%~48%
    const steps = 10;
    const pts = [];
    for (let i = 0; i <= steps; i++) {
      const y = ((i / steps) * 100).toFixed(1);
      const tooth = (i % 2 === 0 ? 1 : -1) * (1.8 + pseudoRandom(seed + i + 1) * 3.4);
      pts.push(`${(tearAt + tooth).toFixed(2)}% ${y}%`);
    }
    const line = pts.join(', ');
    els.shellLeft.style.clipPath = `polygon(0% 0%, ${line}, 0% 100%)`;
    els.shellRight.style.clipPath = `polygon(100% 0%, ${line}, 100% 100%)`;
  }
```

- [ ] **Step 5:`ichiban/js/ui.js` —— `reset()` 換掉 DOM 的部分**

`reset()` 裡 `created.forEach(...)`、`created.clear()`、`running.clear()` 三行保留不動,
底下那幾行換成:

```js
    els.dim.style.opacity = '0';
    els.tearCard.style.opacity = '0';
    els.tearCard.style.transform = 'scale(.4)';
    els.tearCard.removeAttribute('data-bonus');
    for (const shell of [els.shellLeft, els.shellRight]) {
      shell.style.transform = '';
      shell.style.opacity = '1';
    }
    els.aura.style.opacity = '0';
    els.particles.replaceChildren();
    els.cardResult.style.opacity = '0';
    els.cardResult.style.transform = '';
```

- [ ] **Step 6:`ichiban/js/ui.js` —— 碎紙屑從裂痕噴,不是從正中央**

`spawnShreds` 裡這一行:

```js
      bit.style.left = `${50 + (Math.random() * 70 - 35)}%`;
```

換成(碎屑要出在裂痕上):

```js
      bit.style.left = `${tearAt + (Math.random() * 16 - 8)}%`;
```

- [ ] **Step 7:`ichiban/js/ui.js` —— `playHold` 改成操作 `.tear`**

`playHold` 裡 `els.ticketCard` 全部改成 `els.tearCard`,並在 `sfx.drop()` 之前補上撕痕:

```js
      const offset = originOffset(originRect);
      els.tearCard.style.setProperty('--tier-color', color);
      els.tearCard.style.setProperty('--tier-glow', glow);
      applyTearLine(Math.floor(Math.random() * 10000));

      sfx.drop();
      await Promise.all([
        animate(els.dim, [{ opacity: 0 }, { opacity: 1 }], 320),
        animate(els.tearCard, [
          { transform: `translate(${offset.x}px, ${offset.y}px) scale(.4) rotate(-8deg)`, opacity: 0 },
          { transform: 'translate(0,0) scale(1) rotate(0deg)', opacity: 1 },
        ], 420 + level * 30, { easing: 'cubic-bezier(.34,1.2,.64,1)' }),
      ]);
```

`cancelReturn` 裡的 `els.ticketCard` 也一樣改成 `els.tearCard`,其他不動。

- [ ] **Step 8:`ichiban/js/ui.js` —— `playTear` 換成兩段式的撕**

整支 `playTear` 換成:

```js
  // 撕 —— 兩片沿著同一條裂痕往反方向拉開(縫裡露出白紙),再各自飛出畫面。
  // 分成兩段是刻意的:第一段要讓人看見「縫變寬」,那才是「撕」;
  // 直接讓兩片飛走就會退回上一版「掀掉一半」的觀感。
  async function playTear({ name, badgeLabel, bonus }) {
    const { level, color } = pending ?? { level: 0, color: '#E7DFD4' };
    playing = true;
    skipping = false;
    try {
      const spread = 34 + level * 10;
      sfx.crack();
      spawnShreds(color, 3 + level * 4);

      // 第一段:裂痕拉開。
      await Promise.all([
        animate(els.shellLeft, [
          { transform: 'translate(0,0) rotate(0deg)' },
          { transform: `translate(${-spread}px, 5px) rotate(-3deg)` },
        ], 260 + level * 15, { easing: 'cubic-bezier(.25,1.3,.5,1)' }),
        animate(els.shellRight, [
          { transform: 'translate(0,0) rotate(0deg)' },
          { transform: `translate(${spread}px, -5px) rotate(3deg)` },
        ], 260 + level * 15, { easing: 'cubic-bezier(.25,1.3,.5,1)' }),
      ]);

      // 第二段:兩片飛出去,白紙自己留下來。
      await Promise.all([
        animate(els.shellLeft, [
          { transform: `translate(${-spread}px, 5px) rotate(-3deg)`, opacity: 1 },
          { transform: `translate(${-spread - 240}px, 54px) rotate(-28deg)`, opacity: 0 },
        ], 340 + level * 20, { easing: 'cubic-bezier(.4,0,.75,1)' }),
        animate(els.shellRight, [
          { transform: `translate(${spread}px, -5px) rotate(3deg)`, opacity: 1 },
          { transform: `translate(${spread + 240}px, 54px) rotate(28deg)`, opacity: 0 },
        ], 340 + level * 20, { easing: 'cubic-bezier(.4,0,.75,1)' }),
      ]);

      // 賞別等級越高,光暈跟碎花越誇張;G 賞(level 0)乾脆不放光,樸素到底。
      if (level > 0) {
        sfx.burst(Math.min(level - 1, 3));
        spawnParticles(color, 4 + level * 6);
        await flashAura(color, 2.2 + level * .6, 380 + level * 60);
      } else {
        sfx.clunk();
      }

      els.tearCard.dataset.bonus = String(bonus);
      els.cardBadge.textContent = badgeLabel;
      els.cardName.textContent = name;
      sfx.upgrade(Math.min(level, 3));
      await animate(els.cardResult,
        [{ transform: 'translateY(-14px)', opacity: 0 }, { transform: 'translateY(0)', opacity: 1 }],
        260 + level * 20, { easing: 'cubic-bezier(.34,1.5,.64,1)' });
    } finally {
      playing = false;
      skipping = false;
      pending = null;
    }
  }
```

- [ ] **Step 9:`ichiban/js/main.js` —— 只改 els**

`createRevealer({...})` 裡的 `ticketCard: $('ticketCard'), cardTop: $('cardTop'),` 兩行換成:

```js
  tearCard: $('tearCard'),
  shellLeft: $('shellLeft'),
  shellRight: $('shellRight'),
```

其餘(`dim`、`cardResult`、`cardBadge`、`cardName`、`aura`、`particles`)不動。
**`main.js` 只有這一處要改。** 改到別的地方就是做過頭了。

- [ ] **Step 10:跑測試**

Run: `cd /Users/willian/github/gashapon && node --test`
Expected: PASS(126 個,一個都不該少 —— 這一版沒動任何純邏輯)

- [ ] **Step 11:手動確認(必做,而且要真的看)**

```bash
cd /Users/willian/github/gashapon && python3 -m http.server 8000
```

開 `http://localhost:8000/ichiban/`,逐項確認:

1. 桌上的籤紙有粗描邊 + 一圈齒孔,顏色**不會**跟賞別對上(同一張籤重抽前後顏色不變,
   但抽到 A 賞的那張在桌上不是金色)
2. 點一張 → 飛到中央 → 票卡是完整的一片,**看不到裂痕**(兩片的 clip-path 要嚴絲合縫,
   中間不能有一條白線或重疊的深色帶)
3. 左邊緣有半圓缺口
4. 按「撕開」→ **先看得見縫從一條線變寬、白紙從縫裡露出來**,再兩片飛走
5. 撕痕是鋸齒,而且左右兩片的齒**互相咬合**(不是各裂各的)
6. 連撕三次,裂痕的位置與齒形每次不同
7. 撕到一半點畫面 → 直接跳到結果,不會卡在半開的狀態
8. A 賞明顯比 G 賞盛大
9. 抽到最後一張 → 金邊的最後一抽賞
10. 切到別的分頁再切回來 → 不會卡住

- [ ] **Step 12: Commit**

```bash
git add ichiban/
git commit -m "$(cat <<'MSG'
Tear the ichiban ticket along a ragged seam

The old reveal lifted the top half away, which read as a piece falling
off rather than paper tearing. The shell now splits into two fragments
that share one jagged clip-path edge and pull apart, opening a widening
gap that lets the paper underneath show through before they fly off.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
MSG
)"
```

---

## 已知驗不到的範圍

寫進計畫是為了讓執行者不要誤以為自己驗過了:

- **自動化瀏覽器的分頁是背景狀態、rAF 為 0 fps。** 3D 場景與物理模擬的實際觀感在開發過程中**無法驗證**,
  只能驗邏輯與狀態。`gashapon3d` 計畫 Task 2 與 `smash` 計畫 Task 3 的「手動確認」必須由人在前景分頁執行。
- **Safari / WebKit 與真實行動裝置沒有任何自動化覆蓋。** 既有的 2D 扭蛋機在 iOS 上就出現過
  「Reduce Motion 導致演出被略過」與「音效沒聲音」兩個只在 iOS 上發生的問題,
  三個新模式同樣有這個風險。
- **物理手感**(撞擊夠不夠爽、節奏會不會拖)只能靠人玩。測試只能保證規則正確,不能保證好玩。
