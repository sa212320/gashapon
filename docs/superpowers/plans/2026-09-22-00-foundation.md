# 共用核心與首頁 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把現有的單一扭蛋機重組成一個有首頁的小網站骨架,並抽出四個新模式都要站上去的共用層。

**Architecture:** 根目錄變成首頁,`shared/` 放共用層,2D 扭蛋機搬進 `gashapon/`。這一份完成後,網站已經可以正常使用,只是首頁上另外四張卡還是 404。

**Tech Stack:** 原生 ES module、零建置、零 npm 依賴。測試用 Node 內建 `node --test`。

**Spec:** `docs/superpowers/specs/2026-09-22-multi-mode-site-design.md`

**Prerequisite:** 無。這是第一份,其他四份都相依於它。

## Global Constraints

- **零建置**:沒有 build step、沒有 `node_modules`、沒有 `npm install`。`package.json` 一律不新增。
- **唯一的外部依賴**是 three.js,只能出現在 `gashapon3d/index.html` 的 importmap,從 `https://cdn.jsdelivr.net/npm/three@0.169.0/build/three.module.js` 載入。
- 測試一律 `cd /Users/willian/github/gashapon && node --test`,必須零依賴可跑。
- 所有純函式**不就地改動傳進來的東西**,一律回傳新物件。這是現有 `js/state.js` 的既有慣例。
- 讀 localStorage 一律當成不可信:壞掉就回種子資料,**絕不白屏**。
- 文案一律繁體中文;程式碼、識別字、commit message 一律英文。
- commit message 結尾必須有:
  `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`
- 現有 2D 扭蛋機的 45 個測試在整個計畫過程中**不得有任何一個變紅**。

## 確認過的 Model(權威來源;實作若與此牴觸,是計畫的 bug —— 停下來反映,不要就地改模型)

### Entities

- `AppPrefs { soundOn }` —— 全域,獨立 storage key
- 每個模式都有 `setups[]` 與 `activeSetupId`
- 扭蛋機 2D:`Prize { id, name, count, rarity }` → `Capsule { prizeId, drawn }`
- 扭蛋機 3D:`Prize { id, name, count, rarity }` → `Capsule { prizeId, drawn, color }`
- 一番賞:`IchibanPrize { id, name, tier(A~G), count }` + `lastOnePrize` → `Ticket { prizeId, drawn }`
- 阿彌陀籤:`Player { id, name, color }` + `GhostPrize { id, name, count }` → `Ladder`(每局現場產生,不存)
- 大亂鬥:`Fighter { id, name, color, count }` + `items{attack,speed,giant,bomb}` → `Combatant { fighterId, alive }`(不存)

### Cardinality

- `Prize` 1:N `Capsule`、`IchibanPrize` 1:N `Ticket`、`GhostPrize` 1:N 底部格子、
  `Fighter` 1:N `Combatant` —— 全部由 `count` 決定,同一個形狀
- `Player` 1:1 梯子上的一條線
- **`Fighter` 就是隊伍**,`count` 就是隊伍人數。同 `fighterId` 的 `Combatant` 不互相擊飛,
  最後還有人活著的 `fighterId` 獲勝

### Seen vs stored

- 3D 看到球體裡五顏六色的蛋 → **顏色是每顆蛋自己隨機挑的**,跟獎項、跟稀有度都無關
- 一番賞看到「A賞 PVC模型」→ 存的是 `tier: 'A'` 欄位,**不是名字前綴**
- 阿彌陀籤看到大家像賽跑有快有慢 → **誰拿到什麼在起跑前梯子就決定好了**,快慢純粹是演出
- 阿彌陀籤底部補的「銘謝惠顧」空籤是**算出來的**,不存進設定
- 大亂鬥是**唯一**結果不預先決定的模式

### 最可能做錯的四個(以下四句都是錯的)

1. 「一番賞的賞別就是稀有度」 —— **一番賞沒有稀有度,扭蛋機沒有賞別**
2. 「大亂鬥的隊伍是一個獨立實體」 —— 沒有 `Team`,隊伍就是同一筆 `Fighter`
3. 「3D 扭蛋機的蛋色由稀有度決定」 —— 只有 2D 那台是。3D 每顆蛋隨機上色
4. 「阿彌陀籤誰先跑到終點誰拿頭獎」 —— 名次跟獎項完全無關

## File Structure

| 路徑 | 職責 |
|---|---|
| `index.html` | 首頁,五張卡 |
| `css/home.css` | 首頁卡片版面 |
| `shared/css/tokens.css` | 配色變數、按鈕、對話框、卡片、switch |
| `shared/js/ids.js` | `newId(prefix)` |
| `shared/js/roster.js` | `expand` / `needsRebuild` / `entriesChanged` / `countOf` / setups 操作 |
| `shared/js/storage.js` | `createStore({key, schema, sanitize, seed})`、`requestPersistence` |
| `shared/js/prefs.js` | `loadPrefs` / `savePrefs`,含從 `gashapon.v1` 的一次性遷移 |
| `shared/js/sound.js` | 現有音效模組搬過來,不改邏輯 |
| `shared/js/ask.js` | 現有確認框搬過來,不改邏輯 |
| `shared/js/dialog.js` | 設定對話框骨殼 |
| `gashapon/` | 2D 扭蛋機(現有程式搬進來,邏輯不動) |

---

### Task 1: `shared/js/ids.js` 與 `shared/js/roster.js`

**Files:**
- Create: `shared/js/ids.js`
- Create: `shared/js/roster.js`
- Test: `test/roster.test.js`

**Interfaces:**
- Consumes: 無(這是最底層)
- Produces:
  - `newId(prefix: string): string`
  - `expand(entries: {id,count}[], makeItem: (entry, i) => object): object[]`
  - `countOf(entries: {count}[]): number`
  - `remaining(items: {drawn}[]): number`
  - `needsRebuild(before: {id,count}[], after: {id,count}[]): boolean` —— **只比 id 與 count**
  - `entriesChanged(before: object[], after: object[]): boolean` —— 比對全部欄位,給 dirty 判斷用
  - `createSetupList(seed: object): { setups: object[], activeSetupId: string }`
  - `getActive(state): object`
  - `replaceSetup(state, setup): state`
  - `addSetup(state, setup): state`
  - `removeSetup(state, id, makeSeed: () => object): state`

- [ ] **Step 1: 寫失敗的測試**

建立 `test/roster.test.js`:

```js
import test from 'node:test';
import assert from 'node:assert/strict';

import { newId } from '../shared/js/ids.js';
import {
  expand, countOf, remaining, needsRebuild, entriesChanged,
  createSetupList, getActive, replaceSetup, addSetup, removeSetup,
} from '../shared/js/roster.js';

const entry = (id, count, extra = {}) => ({ id, count, name: id, ...extra });

test('newId 產生的 id 不重複,而且帶著前綴', () => {
  const ids = Array.from({ length: 200 }, () => newId('p'));
  assert.equal(new Set(ids).size, 200);
  assert.ok(ids.every(id => id.startsWith('p_')));
});

test('expand 依 count 展開,並把 entry 交給 makeItem', () => {
  const items = expand([entry('a', 3), entry('b', 1)], e => ({ ref: e.id }));
  assert.equal(items.length, 4);
  assert.equal(items.filter(i => i.ref === 'a').length, 3);
  assert.equal(items.filter(i => i.ref === 'b').length, 1);
});

test('expand 的 makeItem 拿得到「這是該項目的第幾個」', () => {
  const items = expand([entry('a', 3)], (e, i) => ({ i }));
  assert.deepEqual(items.map(x => x.i), [0, 1, 2]);
});

test('count 為 0 的項目不產生任何個體', () => {
  assert.equal(expand([entry('a', 0)], () => ({})).length, 0);
});

test('countOf 是所有 count 的總和', () => {
  assert.equal(countOf([entry('a', 3), entry('b', 2)]), 5);
});

test('remaining 只算還沒被抽走的個體', () => {
  assert.equal(remaining([{ drawn: false }, { drawn: true }, { drawn: false }]), 2);
  assert.equal(remaining([]), 0);
});

test('needsRebuild:改 count 或增刪項目才要重建', () => {
  const before = [entry('a', 3), entry('b', 1)];
  assert.equal(needsRebuild(before, [entry('a', 3), entry('b', 1)]), false);
  assert.equal(needsRebuild(before, [entry('a', 4), entry('b', 1)]), true);
  assert.equal(needsRebuild(before, [entry('a', 3)]), true);
  assert.equal(needsRebuild(before, [...before, entry('c', 1)]), true);
});

test('needsRebuild:改名字、改稀有度、改賞別、改顏色都不重建', () => {
  const before = [entry('a', 3, { rarity: 'N', tier: 'C', color: '#fff' })];
  const after = [entry('a', 3, { rarity: 'UR', tier: 'A', color: '#000' })];
  after[0].name = '換了名字';
  assert.equal(needsRebuild(before, after), false);
});

test('needsRebuild:順序不同但內容一樣,不重建', () => {
  const before = [entry('a', 3), entry('b', 1)];
  assert.equal(needsRebuild(before, [entry('b', 1), entry('a', 3)]), false);
});

test('entriesChanged:任何欄位不同都算變了,順序不算', () => {
  const before = [entry('a', 3, { rarity: 'N' })];
  assert.equal(entriesChanged(before, [entry('a', 3, { rarity: 'N' })]), false);
  assert.equal(entriesChanged(before, [entry('a', 3, { rarity: 'UR' })]), true);
  const two = [entry('a', 1), entry('b', 1)];
  assert.equal(entriesChanged(two, [entry('b', 1), entry('a', 1)]), false);
});

test('createSetupList 給一組設定並指向它', () => {
  const state = createSetupList({ id: 's1', name: '第一組' });
  assert.equal(state.setups.length, 1);
  assert.equal(getActive(state).id, 's1');
});

test('addSetup 新增並切過去,原本那組不受影響', () => {
  let state = createSetupList({ id: 's1', name: '第一組', data: 1 });
  state = addSetup(state, { id: 's2', name: '第二組' });
  assert.equal(state.setups.length, 2);
  assert.equal(getActive(state).id, 's2');
  assert.equal(state.setups[0].data, 1);
});

test('replaceSetup 只換掉同 id 的那一組', () => {
  let state = createSetupList({ id: 's1', name: '舊的' });
  state = addSetup(state, { id: 's2', name: '第二組' });
  state = replaceSetup(state, { id: 's1', name: '新的' });
  assert.equal(state.setups.find(s => s.id === 's1').name, '新的');
  assert.equal(state.setups.find(s => s.id === 's2').name, '第二組');
});

test('removeSetup 刪掉正在用的那組時改指到還在的', () => {
  let state = createSetupList({ id: 's1', name: '第一組' });
  state = addSetup(state, { id: 's2', name: '第二組' });
  state = removeSetup(state, 's2', () => ({ id: 'seed', name: '種子' }));
  assert.equal(state.setups.length, 1);
  assert.equal(getActive(state).id, 's1');
});

test('removeSetup 刪到一組都不剩時,用 makeSeed 補一組', () => {
  let state = createSetupList({ id: 's1', name: '第一組' });
  state = removeSetup(state, 's1', () => ({ id: 'seed', name: '種子' }));
  assert.equal(state.setups.length, 1);
  assert.equal(getActive(state).id, 'seed');
});
```

- [ ] **Step 2: 跑測試確認它失敗**

Run: `cd /Users/willian/github/gashapon && node --test`
Expected: FAIL,`Cannot find module '.../shared/js/ids.js'`

- [ ] **Step 3: 寫最小實作**

建立 `shared/js/ids.js`:

```js
let counter = 0;

export function newId(prefix) {
  const rand = globalThis.crypto?.randomUUID?.() ?? Math.random().toString(36).slice(2);
  return `${prefix}_${(counter++).toString(36)}_${rand.slice(0, 8)}`;
}
```

建立 `shared/js/roster.js`:

```js
// 五個模式的「設定項 → 個體」都是同一個形狀:依 count 展開。
// 這裡只處理那個形狀,不知道獎項、參賽者、籤紙有什麼差別。

export function expand(entries, makeItem) {
  const items = [];
  for (const entry of entries) {
    for (let i = 0; i < entry.count; i++) items.push(makeItem(entry, i));
  }
  return items;
}

export function countOf(entries) {
  return entries.reduce((sum, e) => sum + e.count, 0);
}

export function remaining(items) {
  return items.filter(i => !i.drawn).length;
}

// 池子的形狀變了嗎?只在乎「有哪些項目」跟「各幾個」。
// 改名字、改稀有度、改賞別、改顏色都不會改變池子裡有幾個個體,
// 所以不該沒收使用者已經抽掉的進度。
export function needsRebuild(before, after) {
  const key = e => `${e.id}|${e.count}`;
  const a = before.map(key).sort();
  const b = after.map(key).sort();
  if (a.length !== b.length) return true;
  return a.some((k, i) => k !== b[i]);
}

// 內容有沒有動過,給「按確定時要不要做事」用。不在乎排列順序。
export function entriesChanged(before, after) {
  if (before.length !== after.length) return true;
  const key = e => JSON.stringify(Object.keys(e).sort().map(k => [k, e[k]]));
  const a = before.map(key).sort();
  const b = after.map(key).sort();
  return a.some((k, i) => k !== b[i]);
}

export function createSetupList(seed) {
  return { setups: [seed], activeSetupId: seed.id };
}

export function getActive(state) {
  return state.setups.find(s => s.id === state.activeSetupId) ?? state.setups[0];
}

export function replaceSetup(state, setup) {
  return { ...state, setups: state.setups.map(s => (s.id === setup.id ? setup : s)) };
}

export function addSetup(state, setup) {
  return { ...state, setups: [...state.setups, setup], activeSetupId: setup.id };
}

export function removeSetup(state, id, makeSeed) {
  const setups = state.setups.filter(s => s.id !== id);
  if (setups.length === 0) {
    const seed = makeSeed();
    return { ...state, setups: [seed], activeSetupId: seed.id };
  }
  const activeSetupId = setups.some(s => s.id === state.activeSetupId)
    ? state.activeSetupId
    : setups[0].id;
  return { ...state, setups, activeSetupId };
}
```

- [ ] **Step 4: 跑測試確認全綠**

Run: `cd /Users/willian/github/gashapon && node --test`
Expected: PASS,原本的 45 個測試也要全綠(總數 60)

- [ ] **Step 5: Commit**

```bash
git add shared/js/ids.js shared/js/roster.js test/roster.test.js
git commit -m "$(cat <<'MSG'
Add the shared roster primitives

Every mode expands a list of {id, count} entries into individual items —
capsules, tickets, ladder slots, combatants. expand, needsRebuild and the
setup-list operations now live in one place rather than once per mode.

needsRebuild compares only ids and counts, so renaming a prize or
changing its rarity, tier or colour leaves a child's progress alone.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
MSG
)"
```

---

### Task 2: `shared/js/storage.js` 泛用化

**Files:**
- Create: `shared/js/storage.js`
- Test: `test/shared-storage.test.js`

**Interfaces:**
- Consumes: 無
- Produces:
  - `createStore({ key: string, schema: number, sanitize: (parsed) => object|null, seed: () => object })`
    → `{ load(storage?), save(state, storage?), createDebouncedSave(storage?, delay?) }`
  - `requestPersistence(): Promise<boolean>`

`sanitize` 拿到 `JSON.parse` 的結果,回傳整理乾淨的 state,或回 `null` 表示這份存檔不可用。

- [ ] **Step 1: 寫失敗的測試**

建立 `test/shared-storage.test.js`:

```js
import test from 'node:test';
import assert from 'node:assert/strict';

import { createStore } from '../shared/js/storage.js';

function fakeStorage(initial = {}) {
  const map = new Map(Object.entries(initial));
  return {
    getItem: k => (map.has(k) ? map.get(k) : null),
    setItem: (k, v) => map.set(k, String(v)),
    removeItem: k => map.delete(k),
  };
}

const store = createStore({
  key: 'demo.v1',
  schema: 1,
  seed: () => ({ items: ['種子'] }),
  sanitize: parsed => (Array.isArray(parsed.items) ? { items: parsed.items } : null),
});

test('什麼都沒存時回種子資料', () => {
  assert.deepEqual(store.load(fakeStorage()), { items: ['種子'] });
});

test('壞掉的 JSON 回種子資料,不丟例外', () => {
  assert.deepEqual(store.load(fakeStorage({ 'demo.v1': '{壞掉' })), { items: ['種子'] });
});

test('空字串回種子資料', () => {
  assert.deepEqual(store.load(fakeStorage({ 'demo.v1': '' })), { items: ['種子'] });
});

test('schema 版本不認得時回種子資料', () => {
  const raw = JSON.stringify({ schema: 999, items: ['舊的'] });
  assert.deepEqual(store.load(fakeStorage({ 'demo.v1': raw })), { items: ['種子'] });
});

test('sanitize 回 null 時回種子資料', () => {
  const raw = JSON.stringify({ schema: 1, items: '不是陣列' });
  assert.deepEqual(store.load(fakeStorage({ 'demo.v1': raw })), { items: ['種子'] });
});

test('存了再讀,內容一致', () => {
  const s = fakeStorage();
  store.save({ items: ['甲', '乙'] }, s);
  assert.deepEqual(store.load(s), { items: ['甲', '乙'] });
});

test('save 會寫進 schema 版本', () => {
  const s = fakeStorage();
  store.save({ items: [] }, s);
  assert.equal(JSON.parse(s.getItem('demo.v1')).schema, 1);
});

test('空間爆掉時 save 不丟例外,並回 false', () => {
  const s = fakeStorage();
  s.setItem = () => { throw new DOMException('full', 'QuotaExceededError'); };
  let result;
  assert.doesNotThrow(() => { result = store.save({ items: [] }, s); });
  assert.equal(result, false);
});

test('兩個 store 用不同 key,互不干擾', () => {
  const other = createStore({
    key: 'other.v1', schema: 1,
    seed: () => ({ n: 0 }),
    sanitize: p => (typeof p.n === 'number' ? { n: p.n } : null),
  });
  const s = fakeStorage();
  store.save({ items: ['甲'] }, s);
  other.save({ n: 7 }, s);
  assert.deepEqual(store.load(s), { items: ['甲'] });
  assert.deepEqual(other.load(s), { n: 7 });
});
```

- [ ] **Step 2: 跑測試確認它失敗**

Run: `cd /Users/willian/github/gashapon && node --test`
Expected: FAIL,`Cannot find module '.../shared/js/storage.js'`

- [ ] **Step 3: 寫最小實作**

建立 `shared/js/storage.js`:

```js
// localStorage 讀寫。原則:讀進來的東西一律當成不可信,
// 壞掉就回種子資料,絕不讓小孩看到白畫面。

export function createStore({ key, schema, sanitize, seed }) {
  function load(storage = globalThis.localStorage) {
    try {
      const raw = storage?.getItem(key);
      if (!raw) return seed();
      const parsed = JSON.parse(raw);
      if (!parsed || parsed.schema !== schema) return seed();
      return sanitize(parsed) ?? seed();
    } catch {
      return seed();
    }
  }

  function save(state, storage = globalThis.localStorage) {
    try {
      storage?.setItem(key, JSON.stringify({ schema, ...state }));
      return true;
    } catch {
      // 配額爆掉或被瀏覽器擋住:功能照常,只是這次沒存到
      return false;
    }
  }

  // 打字時不要每個按鍵都寫一次 localStorage
  function createDebouncedSave(storage = globalThis.localStorage, delay = 200) {
    let timer = null;
    return state => {
      clearTimeout(timer);
      timer = setTimeout(() => save(state, storage), delay);
    };
  }

  return { key, load, save, createDebouncedSave };
}

// 請瀏覽器把這個站的資料標成「持久」,避免空間吃緊時被驅逐。
// 拿不到就算了,不影響任何功能。
export async function requestPersistence() {
  try {
    if (!navigator.storage?.persist) return false;
    if (await navigator.storage.persisted()) return true;
    return await navigator.storage.persist();
  } catch {
    return false;
  }
}
```

- [ ] **Step 4: 跑測試確認全綠**

Run: `cd /Users/willian/github/gashapon && node --test`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shared/js/storage.js test/shared-storage.test.js
git commit -m "$(cat <<'MSG'
Generalise the store so every mode can have its own key

createStore takes a key, a schema version, a sanitiser and a seed, and
hands back load/save/debouncedSave. The rule the original storage module
established survives: anything read back is untrusted, and a corrupt
record yields seed data rather than a blank screen.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
MSG
)"
```

---

### Task 3: `shared/js/prefs.js` 與 soundOn 遷移

**Files:**
- Create: `shared/js/prefs.js`
- Test: `test/prefs.test.js`

**Interfaces:**
- Consumes: `createStore` from `shared/js/storage.js`
- Produces:
  - `loadPrefs(storage?): { soundOn: boolean }`
  - `savePrefs(prefs, storage?): boolean`

遷移規則:`prefs.v1` 不存在時,若 `gashapon.v1` 解析得出 `soundOn === false`,新的 `soundOn` 就是 `false`;
其餘情況一律 `true`。舊 key 裡的 `soundOn` 欄位**留著不動**(不去改它,避免動到 2D 的存檔)。

- [ ] **Step 1: 寫失敗的測試**

建立 `test/prefs.test.js`:

```js
import test from 'node:test';
import assert from 'node:assert/strict';

import { loadPrefs, savePrefs } from '../shared/js/prefs.js';

function fakeStorage(initial = {}) {
  const map = new Map(Object.entries(initial));
  return {
    getItem: k => (map.has(k) ? map.get(k) : null),
    setItem: (k, v) => map.set(k, String(v)),
    removeItem: k => map.delete(k),
    has: k => map.has(k),
  };
}

test('全新的瀏覽器:音效預設是開的', () => {
  assert.equal(loadPrefs(fakeStorage()).soundOn, true);
});

test('存了再讀,音效開關留著', () => {
  const s = fakeStorage();
  savePrefs({ soundOn: false }, s);
  assert.equal(loadPrefs(s).soundOn, false);
});

test('遷移:舊的 gashapon.v1 關了音效,新的 prefs 也要是關的', () => {
  const s = fakeStorage({
    'gashapon.v1': JSON.stringify({ schema: 1, soundOn: false, machines: [], activeMachineId: 'x' }),
  });
  assert.equal(loadPrefs(s).soundOn, false);
});

test('遷移:舊的 gashapon.v1 音效是開的,新的也是開的', () => {
  const s = fakeStorage({
    'gashapon.v1': JSON.stringify({ schema: 1, soundOn: true, machines: [], activeMachineId: 'x' }),
  });
  assert.equal(loadPrefs(s).soundOn, true);
});

test('遷移:舊存檔壞掉時不丟例外,當成開著', () => {
  assert.equal(loadPrefs(fakeStorage({ 'gashapon.v1': '{壞掉' })).soundOn, true);
});

test('遷移只在 prefs 還沒建立時發生 —— 已經有 prefs 就以 prefs 為準', () => {
  const s = fakeStorage({
    'gashapon.v1': JSON.stringify({ schema: 1, soundOn: false }),
  });
  savePrefs({ soundOn: true }, s);
  assert.equal(loadPrefs(s).soundOn, true);
});

test('遷移不會去改動舊的 gashapon.v1', () => {
  const before = JSON.stringify({ schema: 1, soundOn: false, machines: [], activeMachineId: 'x' });
  const s = fakeStorage({ 'gashapon.v1': before });
  loadPrefs(s);
  assert.equal(s.getItem('gashapon.v1'), before);
});
```

- [ ] **Step 2: 跑測試確認它失敗**

Run: `cd /Users/willian/github/gashapon && node --test`
Expected: FAIL,`Cannot find module '.../shared/js/prefs.js'`

- [ ] **Step 3: 寫最小實作**

建立 `shared/js/prefs.js`:

```js
// 全站共用的偏好。目前只有音效開關。
import { createStore } from './storage.js';

const LEGACY_KEY = 'gashapon.v1';

// soundOn 原本住在 2D 扭蛋機的存檔裡。搬家時讀一次舊的,
// 之後就以 prefs.v1 為準。舊 key 的欄位留著不動,免得動到 2D 的存檔。
function migrate(storage) {
  try {
    const raw = storage?.getItem(LEGACY_KEY);
    if (!raw) return { soundOn: true };
    const parsed = JSON.parse(raw);
    return { soundOn: parsed?.soundOn !== false };
  } catch {
    return { soundOn: true };
  }
}

let migrationStorage = null;

const store = createStore({
  key: 'prefs.v1',
  schema: 1,
  seed: () => migrate(migrationStorage),
  sanitize: parsed => ({ soundOn: parsed.soundOn !== false }),
});

export function loadPrefs(storage = globalThis.localStorage) {
  migrationStorage = storage;
  return store.load(storage);
}

export function savePrefs(prefs, storage = globalThis.localStorage) {
  return store.save(prefs, storage);
}
```

- [ ] **Step 4: 跑測試確認全綠**

Run: `cd /Users/willian/github/gashapon && node --test`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add shared/js/prefs.js test/prefs.test.js
git commit -m "$(cat <<'MSG'
Move the sound switch to a site-wide preference

soundOn used to live inside the machine's own record; four modes share it
now, so it gets its own key. Existing visitors keep whatever they chose:
the first read falls back to the old record, and never writes to it.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
MSG
)"
```

---

### Task 4: 把 2D 扭蛋機搬進 `gashapon/`,改用共用層

**Files:**
- Create: `gashapon/index.html`(從根目錄的 `index.html` 搬,`<link>`/`<script>` 路徑改為 `../shared/…` 與 `js/…`)
- Create: `gashapon/js/{main,constants,state,gacha,reveal,storage,ui-machine,ui-settings}.js`(從 `js/` 搬)
- Create: `gashapon/css/{style,animations}.css`(從 `css/` 搬)
- Create: `shared/js/sound.js`、`shared/js/ask.js`(從 `js/` 搬,內容不改)
- Delete: 根目錄的 `js/`、`css/`、`index.html`
- Modify: `test/state.test.js`、`test/gacha.test.js`、`test/storage.test.js` —— 只改 import 路徑
- Modify: `check.html` —— `./js/gacha.js` 改成 `./gashapon/js/gacha.js`,`./js/sound.js` 改成 `./shared/js/sound.js`

**Interfaces:**
- Consumes: 只有它自己搬過去的 `shared/js/sound.js` 與 `shared/js/ask.js`。**這一 task 不改任何邏輯**,所以不消費 `createStore` / `loadPrefs` / `needsRebuild` —— 那些是後續整理的事
- Produces: `gashapon/` 這個可獨立運作的模式頁

**這一 task 不改任何邏輯。** 45 個既有測試必須原封不動地全綠(只改 import 路徑)。

⚠️ **`js/storage.js` 一定要跟著搬。** 它被 `js/main.js` 與 `test/storage.test.js` import,
漏掉的話 `test/storage.test.js` 整個檔案載不起來(45 → 35),扭蛋機的頁面也會白屏。
它搬完之後叫 `gashapon/js/storage.js`,跟 Task 2 建立的 `shared/js/storage.js` **是兩個不同的東西**:
前者是 2D 扭蛋機自己的存檔邏輯(含它專屬的 sanitize),後者是泛用的 `createStore`。
把前者改寫成建在後者之上是合理的後續整理,但**不在這個 task 的範圍**,因為那會改到邏輯。

- [ ] **Step 1: 搬檔案**

```bash
cd /Users/willian/github/gashapon
mkdir -p gashapon/js gashapon/css shared/js
git mv js/sound.js shared/js/sound.js
git mv js/ask.js shared/js/ask.js
git mv js/main.js js/constants.js js/state.js js/gacha.js js/reveal.js js/storage.js js/ui-machine.js js/ui-settings.js gashapon/js/
git mv css/style.css css/animations.css gashapon/css/
git mv index.html gashapon/index.html
```

- [ ] **Step 2: 修正路徑**

`gashapon/index.html`:`href="css/style.css"` 與 `href="css/animations.css"` 不變(相對路徑仍然對),
`src="js/main.js"` 不變。新增一行在 `css/style.css` 之前:

```html
<link rel="stylesheet" href="../shared/css/tokens.css">
```

(`tokens.css` 在 Task 5 才建立,先建一個空檔 `touch shared/css/tokens.css` 讓路徑有效。)

`gashapon/js/reveal.js` 與 `gashapon/js/main.js` 裡的 `from './sound.js'` 改成 `from '../../shared/js/sound.js'`;
`gashapon/js/main.js` 裡的 `from './ask.js'` 改成 `from '../../shared/js/ask.js'`。

三個測試檔的 import 從 `../js/x.js` 改成 `../gashapon/js/x.js`。

`check.html` 裡 `import('./js/gacha.js')` 改成 `import('./gashapon/js/gacha.js')`,
`from './js/sound.js'` 改成 `from './shared/js/sound.js'`。

- [ ] **Step 3: 跑測試確認仍然全綠**

Run: `cd /Users/willian/github/gashapon && node --test`
Expected: PASS,45 + Task 1~3 新增的測試全部通過。**一個都不能紅。**

- [ ] **Step 4: 手動確認頁面還會動**

```bash
cd /Users/willian/github/gashapon && python3 -m http.server 8000
```

開 `http://localhost:8000/gashapon/`,確認:機台名稱與剩餘顆數有出現、圓頂裡有蛋、按「轉!」會跑完整演出、設定三個分頁都開得起來。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "$(cat <<'MSG'
Move the 2D machine into its own folder

Pure relocation ahead of the other modes arriving: no logic changes, and
the existing tests pass unmodified apart from their import paths. sound
and ask move to shared/ because every mode will want them.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
MSG
)"
```

---

### Task 5: `shared/css/tokens.css` 與首頁

**Files:**
- Create: `shared/css/tokens.css`
- Create: `index.html`(新的首頁)
- Create: `css/home.css`
- Modify: `gashapon/css/style.css` —— 刪掉已經搬進 tokens 的變數與按鈕規則

**Interfaces:**
- Consumes: 無
- Produces: CSS 變數 `--cream --cream-deep --ink --coral --coral-deep --sun --blush --mint --stroke --radius --rainbow --rainbow-conic` 與 class `.btn .btn--primary .btn--ghost .btn--block .icon-btn .dialog .dialog__head .dialog__body .dialog__foot .tabs .tab .panel .field .switch .chip`

- [ ] **Step 1: 抽出 tokens.css**

把 `gashapon/css/style.css` 的以下區段整段搬到 `shared/css/tokens.css`:`:root` 變數區、
`* { box-sizing }`、`[hidden] { display: none !important }`、`.btn` 系列、`.icon-btn`、
`.dialog` 系列、`.tabs`/`.tab`/`.panel`、`.field`、`.switch`、`.chip`。
`gashapon/css/style.css` 只留扭蛋機自己的東西(`.machine__*`、`.capsule`、`.aura`、`.prize-card`、
`.particle`、`.empty-state`、`.prize-row`、`.edit-card`、`.rarity-picker`、`.stepper`)。

- [ ] **Step 2: 寫首頁**

建立 `index.html`:

```html
<!doctype html>
<html lang="zh-Hant">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
<meta name="theme-color" content="#FFF7EC">
<title>抽獎小工具</title>
<link rel="stylesheet" href="shared/css/tokens.css">
<link rel="stylesheet" href="css/home.css">
</head>
<body>
<header class="home-head">
  <h1>要玩哪一個?</h1>
</header>
<main class="home-grid">
  <a class="card" href="gashapon/">
    <span class="card__art card__art--gashapon" aria-hidden="true"></span>
    <span class="card__name">扭蛋機</span>
    <span class="card__desc">搖越多次越大獎</span>
  </a>
  <a class="card" href="gashapon3d/">
    <span class="card__art card__art--gashapon3d" aria-hidden="true"></span>
    <span class="card__name">立體扭蛋機</span>
    <span class="card__desc">看蛋在球裡滾來滾去</span>
  </a>
  <a class="card" href="ichiban/">
    <span class="card__art card__art--ichiban" aria-hidden="true"></span>
    <span class="card__name">一番賞</span>
    <span class="card__desc">抽一張,翻開看是幾賞</span>
  </a>
  <a class="card" href="ghostleg/">
    <span class="card__art card__art--ghostleg" aria-hidden="true"></span>
    <span class="card__name">阿彌陀籤</span>
    <span class="card__desc">沿著線一路跑到終點</span>
  </a>
  <a class="card" href="smash/">
    <span class="card__art card__art--smash" aria-hidden="true"></span>
    <span class="card__name">大亂鬥</span>
    <span class="card__desc">撞出場外就淘汰</span>
  </a>
</main>
</body>
</html>
```

`css/home.css` 用 `grid-template-columns: repeat(auto-fit, minmax(150px, 1fr))`,卡片沿用 tokens 的粗描邊與立體陰影風格,觸控目標至少 44px。
`card__art` 各自用 CSS 畫一個簡單圖示(圓形、方塊、線條),不引任何圖檔。

- [ ] **Step 3: 手動確認**

```bash
cd /Users/willian/github/gashapon && python3 -m http.server 8000
```

開 `http://localhost:8000/`,確認五張卡都在、點「扭蛋機」進得去、手機寬度下不會橫向捲動。
(其餘四個模式此時還是 404,是預期的。)

- [ ] **Step 4: 跑測試**

Run: `cd /Users/willian/github/gashapon && node --test`
Expected: PASS(CSS 與 HTML 不影響測試,這一步是確認沒有誤刪 js)

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "$(cat <<'MSG'
Add the home page and lift the shared styling out

The root now lists the five modes instead of being the machine itself.
Colour tokens, buttons, dialogs and form controls move to shared/, so the
machine's own stylesheet is left with only the machine.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
MSG
)"
```

---

### Task 6: `shared/js/dialog.js` 設定對話框骨殼

**Files:**
- Create: `shared/js/dialog.js`
- Modify: `gashapon/js/ui-settings.js` —— 改用骨殼(見 Step 2)
- Test: 無自動測試(純 DOM 接線);由後續四份模式計畫的手動驗證覆蓋

**Interfaces:**
- Consumes: `entriesChanged` from `shared/js/roster.js`
- Produces:
  - `createDialogShell({ els, ask, actions, tabs })` → `{ open(), close() }`
  - `els` 需含:`dialog, setupSelect, addSetupBtn, deleteSetupBtn, tabsNav, confirmBtn, cancelBtn, closeBtn`
  - `actions` 需含:`getState(), getActiveSetup(), switchSetup(id), addSetup(), deleteSetup(id), snapshot(), isDirty(), applyDraft(), renderPanels()`
  - `tabs`:`[{ name, panelEl }]`,第一個是預設分頁

骨殼只負責:分頁切換、設定組下拉的渲染與三個按鈕、開啟時呼叫 `snapshot()`、
按確定時呼叫 `applyDraft()` 後關閉、切換/新增設定組時若 `isDirty()` 為真先用 `ask()` 確認。
**什麼欄位算 dirty、確定時要不要重建池子,全部由各模式自己的 `isDirty` 與 `applyDraft` 決定。**

- [ ] **Step 1: 寫實作**

建立 `shared/js/dialog.js`:

```js
// 設定對話框的骨殼。分頁、設定組下拉、取消/確定、dirty 確認 —— 五個模式都一樣。
// 什麼欄位算 dirty、按確定時要不要重建池子,全部由各模式的 isDirty / applyDraft 決定,
// 這裡一概不知道。

export function createDialogShell({ els, ask, actions, tabs }) {
  function renderSetupPicker() {
    const state = actions.getState();
    els.setupSelect.replaceChildren(...state.setups.map(setup => {
      const opt = document.createElement('option');
      opt.value = setup.id;
      opt.textContent = setup.name || '沒有名字的設定';
      opt.selected = setup.id === state.activeSetupId;
      return opt;
    }));
    els.deleteSetupBtn.disabled = state.setups.length <= 1;
  }

  function showTab(name) {
    els.tabsNav.querySelectorAll('.tab').forEach(t => {
      t.classList.toggle('is-active', t.dataset.tab === name);
    });
    for (const tab of tabs) tab.panelEl.hidden = tab.name !== name;
  }

  function refresh() {
    renderSetupPicker();
    actions.renderPanels();
  }

  els.tabsNav.addEventListener('click', e => {
    const tab = e.target.closest('.tab');
    if (tab) showTab(tab.dataset.tab);
  });

  els.setupSelect.addEventListener('change', async () => {
    const id = els.setupSelect.value;
    if (actions.isDirty() && !await ask('還沒按確定的修改會不見喔,要換一組嗎?')) {
      renderSetupPicker();
      return;
    }
    actions.switchSetup(id);
    actions.snapshot();
    refresh();
  });

  els.addSetupBtn.addEventListener('click', async () => {
    if (actions.isDirty() && !await ask('還沒按確定的修改會不見喔,要新增一組嗎?')) return;
    actions.addSetup();
    actions.snapshot();
    refresh();
    showTab(tabs.at(-1).name);
  });

  els.deleteSetupBtn.addEventListener('click', async () => {
    const setup = actions.getActiveSetup();
    if (!await ask(`要刪掉「${setup.name || '這一組'}」嗎?刪掉就找不回來了。`)) return;
    actions.deleteSetup(setup.id);
    actions.snapshot();
    refresh();
  });

  els.confirmBtn.addEventListener('click', async () => {
    // applyDraft 自己決定要不要先問、以及要不要重建池子。
    // 回 false 表示它決定不關對話框(例如使用者在重建確認時按了不要)。
    const applied = await actions.applyDraft();
    if (applied !== false) els.dialog.close();
  });

  els.cancelBtn.addEventListener('click', () => els.dialog.close());
  els.closeBtn.addEventListener('click', () => els.dialog.close());

  return {
    open() {
      actions.snapshot();
      refresh();
      showTab(tabs[0].name);
      els.dialog.showModal();
    },
    close() { els.dialog.close(); },
    refresh,
  };
}
```

`applyDraft()` 回 `false` 代表「不要關對話框」—— 這是使用者在「會重新裝滿喔」確認時按了不要的情況。

- [ ] **Step 2: 讓 2D 扭蛋機改用骨殼**

改寫 `gashapon/js/ui-settings.js`,把分頁切換、設定組下拉、三顆按鈕的接線交給 `createDialogShell`,
只保留三個分頁各自的渲染與 `isDirty` / `applyDraft`。

- [ ] **Step 3: 跑測試**

Run: `cd /Users/willian/github/gashapon && node --test`
Expected: PASS(45 + 新增的全部)

- [ ] **Step 4: 手動確認 2D 扭蛋機的設定行為沒有退化**

開 `http://localhost:8000/gashapon/`,逐項確認:
只看不改按確定 → 池子不動;改名字按確定 → 不跳確認、進度留著;改數量按確定 → 跳「會重新裝滿」確認;
新增設定組 → 切過去且原本那組進度還在;刪到剩一組時刪除鍵變灰。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "$(cat <<'MSG'
Extract the settings dialog shell

Tabs, the setup dropdown, cancel/confirm and the dirty prompt are the same
in all five modes. The shell owns those; each mode keeps only its own
panels and its own answer to what counts as a change.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
MSG
)"
```

---

---

## 已知驗不到的範圍

寫進計畫是為了讓執行者不要誤以為自己驗過了:

- **自動化瀏覽器的分頁是背景狀態、rAF 為 0 fps。** 3D 場景與物理模擬的實際觀感在開發過程中**無法驗證**,
  只能驗邏輯與狀態。`gashapon3d` 計畫 Task 2 與 `smash` 計畫 Task 3 的「手動確認」必須由人在前景分頁執行。
- **Safari / WebKit 與真實行動裝置沒有任何自動化覆蓋。** 既有的 2D 扭蛋機在 iOS 上就出現過
  「Reduce Motion 導致演出被略過」與「音效沒聲音」兩個只在 iOS 上發生的問題,
  三個新模式同樣有這個風險。
- **物理手感**(撞擊夠不夠爽、節奏會不會拖)只能靠人玩。測試只能保證規則正確,不能保證好玩。
